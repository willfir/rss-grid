# rss-grid
A simple website that displays RSS feeds in a grid


## Usage
Run main.py.

The RSS feeds displayed can be configured in `config/feeds.json`. Some elements of the site (title, upper-right 
navbar links) are in `config/main.json`